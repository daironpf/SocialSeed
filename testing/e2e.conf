general:
  environment: dev
  timeout: 30000
  verbose: True
  user_agent: socialseed-e2e/1.0
  project_name: SocialSeed

services:
  auth_service:
    base_url: http://localhost:8085
    health_endpoint: /actuator/health
    timeout: 30000
  auth-service:
    base_url: http://localhost:8080
    health_endpoint: /health
    timeout: 30000